# GitHub Upload Commands

From Git Bash on Windows:

```bash
cd /c/Carrera_Ciberseguridad/06_Portfolio_GitHub/mainframe-racf-security-evidence

git status
git add h1-racf-sandbox-safe-dataset-profiles
git commit -m "Add H1 RACF sandbox dataset profiles lab"
git push
```

If the repository for the new hardening phase is separate, copy this lab folder there and use the same add/commit/push workflow.
