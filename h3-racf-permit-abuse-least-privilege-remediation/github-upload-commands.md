# GitHub Upload Commands

Use Git Bash:

```bash
cd /c/Carrera_Ciberseguridad/06_Portfolio_GitHub/mainframe-racf-security-evidence

git status
git add h3-racf-permit-abuse-least-privilege-remediation
git commit -m "Add H3 RACF PERMIT abuse least privilege remediation lab"
git push
```

If the lab is still in Downloads, copy or extract it into the repository root first.
