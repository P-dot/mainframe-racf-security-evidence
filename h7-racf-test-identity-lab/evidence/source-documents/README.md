# Source documents

The working Word evidence files used to build this lab were uploaded during the session as `H7.docx`, `H7(1).docx`, `H7(2).docx`, and `H7(3).docx`.

They are not included in this GitHub-ready lab package to avoid publishing temporary logon/password setup material from the raw command history. The extracted screenshots are included under `evidence/screenshots/`.
