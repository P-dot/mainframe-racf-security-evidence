# Commands executed

Todos los comandos fueron de solo lectura. No se aplicó hardening ni cambios RACF.

## Lab 09 — zFS backing dataset review

SDSF / consola:

```text
/D OMVS,F
```

TSO / ISPF option 6:

```text
LISTDSD DA('DFH410.ZFS') ALL
LISTDSD DA('DSN910.SJVAZFS') ALL
LISTDSD DA('DSN910.SDSNGLS') ALL

SEARCH CLASS(DATASET) MASK(DFH410.*)
SEARCH CLASS(DATASET) MASK(DSN910.*)
```

## Lab 10 — SAF / FACILITY / UNIXPRIV / SERVAUTH review

TSO / ISPF option 6:

```text
SETROPTS LIST

SEARCH CLASS(FACILITY) MASK(BPX*)
SEARCH CLASS(UNIXPRIV) MASK(*)

SEARCH CLASS(SERVAUTH) MASK(EZB*)
SEARCH CLASS(SERVAUTH) MASK(EZB.NETSTAT*)
SEARCH CLASS(SERVAUTH) MASK(EZB.STACKACCESS*)
SEARCH CLASS(SERVAUTH) MASK(EZB.STACKACCESS*)
SEARCH CLASS(SERVAUTH) MASK(EZB.PORTACCESS*)
SEARCH CLASS(SERVAUTH) MASK(EZB.PAGENT*)

SEARCH CLASS(STARTED) MASK(TCPIP)
RLIST STARTED TCPIP.* ALL
SEARCH CLASS(STARTED) MASK(FTPD*)
SEARCH CLASS(STARTED) MASK(SSHD*)
SEARCH CLASS(STARTED) MASK(INETD*)
```

## Commands deliberately not executed

```text
ADDSD
RDEFINE
RALTER
PERMIT
SETROPTS RACLIST(...) REFRESH
ALTUSER
ALTGROUP
CONNECT
CHMOD
CHOWN
```

This was an audit-only baseline.
