# GitHub Upload Commands

Run from Git Bash on Windows:

```bash
cd /c/Carrera_Ciberseguridad/06_Portfolio_GitHub/mainframe-racf-security-evidence

git status
git add lab-13-apf-program-authorized-libraries-review
git commit -m "Add APF PROGRAM authorized libraries review lab"
git push
```

Verify:

```bash
git status
git log --oneline -5
```
