# GitHub Upload Commands

```bash
cd /c/Carrera_Ciberseguridad/06_Portfolio_GitHub/mainframe-racf-security-evidence

git status
git add lab-14-final-racf-omvs-security-audit-report
git commit -m "Add final RACF OMVS security audit report"
git push
```

Optional after pushing:

```bash
git status
git log --oneline -5
```
